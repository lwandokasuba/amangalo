# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo 'VITE_API_KEY=${{ secrets.FIREBASE_DEV_API_KEY }}' > .env && echo 'ITE_AUTH_DOMAIN=${{ secrets.FIREBASE_DEV_AUTH_DOMAIN }}' > .env && echo 'VITE_DATABASE_URL=${{ secrets.FIREBASE_DEV_DATABASE_URL }}' > .env && echo 'VITE_PROJECT_ID=${{ secrets.FIREBASE_DEV_PROJECT_ID }}' > .env && echo VITE_STORAGE_BUCKET=${{ secrets.FIREBASE_DEV_STORAGE_BUCKET }}' > .env && echo 'VITE_MESSAGING_SENDER=${{ secrets.FIREBASE_DEV_MESSAGING_SENDER }}' > .env && echo 'VITE_APP_ID=${{ secrets.FIREBASE_DEV_APP_ID }}' > .env && npm install -f && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_OPEN_SOURCE_ZED }}'
          channelId: live
          projectId: open-source-zed
