# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: touch .env.production && echo "VITE_API_KEY=${{ secrets.FIREBASE_DEV_API_KEY }}" >> .env.production && echo "VITE_AUTH_DOMAIN=${{ secrets.FIREBASE_DEV_AUTH_DOMAIN }}" >> .env.production && echo "VITE_DATABASE_URL=${{ secrets.FIREBASE_DEV_DATABASE_URL }}" >> .env.production && echo "VITE_PROJECT_ID=${{ secrets.FIREBASE_DEV_PROJECT_ID }}" >> .env.production && echo "VITE_STORAGE_BUCKET=${{ secrets.FIREBASE_DEV_STORAGE_BUCKET }}" >> .env.production && echo "VITE_MESSAGING_SENDER=${{ secrets.FIREBASE_DEV_MESSAGING_SENDER }}" >> .env.production && echo "VITE_APP_ID=${{ secrets.FIREBASE_DEV_APP_ID }}" >> .env.production && npm install -f && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_OPEN_SOURCE_ZED }}'
          projectId: open-source-zed
